# 1. Formal model

Let a bounded worldline window be:

```text
L_t = <carrier, window, D, I, K, W, P, effects, corrections, lineage>
```

Semantic continuity is:

```text
C_t = D I K W P = 11111
```

Generative direction is a non-compensatory vector:

```text
V_t = <reality_contact,
       difference_uptake,
       agency_distribution,
       reciprocity_return,
       generativity,
       corrigibility,
       resource_regeneration,
       affected_voice,
       future_options,
       source_lineage,
       domination_reduction,
       cross_carrier_cooperation>
```

No scalar sum is authoritative. A negative irreversible-harm or domination dimension cannot be offset by efficiency, novelty or popularity.

A zombie closure is detected when semantic continuity is explicit while repeated cycles exhibit no external difference, no observed world effect, no accepted correction and no transferable return.
